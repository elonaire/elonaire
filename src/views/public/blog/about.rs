use leptos::prelude::*;
use leptos_meta::*;

#[island]
pub fn AboutBlog() -> impl IntoView {
    view! {
        <Title text="About Blog"/>

        <main>
            <div class="min-h-svh flex flex-col gap-[40px] bg-contrast-white relative">
            </div>
        </main>
    }
}
